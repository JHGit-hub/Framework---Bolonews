{# templates/article/fragments/_search_btn_bar.html.twig #}
{% for categorie in categories %}
    <div class="cta">
        <a href="{{ path('app_article', {'categorieId' : categorie.id}) }}" class="primary-btn">{{ categorie.name }}</a>
    </div>
{% endfor %}